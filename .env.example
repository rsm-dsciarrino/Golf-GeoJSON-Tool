MAPBOX_TOKEN=pk.your_token_here
# Postgres connection string for the Supabase project (Settings → Database → Connection string → URI).
# Used by the MCP server's upload_to_supabase tool. Keep the real value in .env (gitignored).
SUPABASE_CONNECTION_STRING=postgresql://postgres.<ref>:<password>@<host>.pooler.supabase.com:5432/postgres
